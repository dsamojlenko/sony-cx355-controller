// Copy this file to secrets.h and fill in your WiFi credentials
// DO NOT commit secrets.h to version control

#pragma once

// WiFi credentials
#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Optional: hardcoded backend address (used if mDNS fails)
// Leave empty to require mDNS discovery
#define BACKEND_HOST ""
#define BACKEND_PORT 3000
